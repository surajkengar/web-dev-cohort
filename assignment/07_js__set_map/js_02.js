// array like object
// array like object is an object that looks like array but it is not array.
//it has a index element 
//it has length property
//but it do not have array method unless we convert them into real array.

const str = "suraj";
console.log(str.length);
 console.log(str[4]);
